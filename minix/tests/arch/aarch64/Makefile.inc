# Architecture-specific tests for aarch64. None yet: the earm ones exercise
# segfault reporting and unaligned access, and both want a running system
# to be written against.

.PATH: ${.CURDIR}/arch/${MACHINE_ARCH}
